// Placeholder — middleware guarantees auth + age attestation by the time this renders.
// Token-paste → sealToken() → bot_instances write lands with the instance-creation flow.
export default function NewInstancePage() {
  return (
    <main style={{ maxWidth: 480, margin: "4rem auto" }}>
      <h1>New bot instance</h1>
      <p>Coming soon.</p>
    </main>
  );
}
